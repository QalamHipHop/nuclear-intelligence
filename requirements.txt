# Nuclear Intelligence v5.0 - HuggingFace Space Requirements
# Optimized for HF Spaces (Python 3.11)
# Tested with Gradio 4.36.0 (last reliable on Py 3.11) — pinned below.
#
# ⚠️  CRITICAL PIN: huggingface-hub MUST stay < 0.26 because Gradio 4.36
#     imports HfFolder from huggingface_hub, which was removed in 0.26.
#     See: https://github.com/gradio-app/gradio/pull/8204
huggingface-hub>=0.22,<0.26

# ─── Core Framework ──────────────────────────────────────────────
gradio[oauth]==4.36.0
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0

# ─── Visualization ──────────────────────────────────────────────
plotly>=5.19.0

# ─── LLM & API Providers ────────────────────────────────────────
openai>=1.12.0,<2.0.0
httpx>=0.27.0
requests>=2.31.0

# ─── Free Web Search (for RAG) ───────────────────────────────────
# `duckduckgo-search` was renamed to `ddgs`. Try `ddgs` first; fall back
# to the legacy package if it's not available. The warning about the
# rename is harmless but noisy.
ddgs>=6.0.0
duckduckgo-search>=6.0.0,<9.0.0

# ─── Utilities & Logging ────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.2
psutil>=5.9.0

# ─── HuggingFace & GitHub Sync ──────────────────────────────────
PyGithub>=2.3.0

# ─── Blockchain & Security ──────────────────────────────────────
cryptography>=42.0.0

# ─── HTML Parsing (RAG web pages) ───────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.2.0

# ─── Async HTTP ─────────────────────────────────────────────────
aiohttp>=3.9.0
websockets>=10.0,<13.0

# ─── Optional FAISS (gracefully skipped if missing) ─────────────
# faiss-cpu>=1.7.4

python-telegram-bot>=21.0,<24.0
