# Nuclear Intelligence v4.0 - HuggingFace Space Requirements
# Optimized for HF Spaces (Python 3.11, Docker)
# Keep minimal: install size and build time matter.

# Core Framework
# Pin the known-compatible pair: Gradio 4.36 imports HfFolder, removed in huggingface-hub 0.26+.
gradio==4.36.0
huggingface-hub>=0.22,<0.26
pandas>=2.2.0,<3.0.0

# LLM & API
openai>=1.12.0,<2.0.0
httpx>=0.27.0
requests>=2.31.0
python-dotenv>=1.0.0

# Blockchain & Security
cryptography>=42.0.0

# Utilities & Monitoring
loguru>=0.7.2
plotly>=5.19.0

# HuggingFace
# Kept pinned above; do not loosen beyond 0.25 while Gradio 4.36 is used.

# PyGithub used by sync_huggingface.py (imported lazily)
PyGithub>=2.3.0